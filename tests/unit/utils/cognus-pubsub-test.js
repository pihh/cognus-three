import cognusPubsub from 'bluedarwin-the-movie/utils/cognus-pubsub';
import { module, test } from 'qunit';

module('Unit | Utility | cognus-pubsub', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = cognusPubsub();
    assert.ok(result);
  });
});
