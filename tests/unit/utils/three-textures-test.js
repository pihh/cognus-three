import threeTextures from 'bluedarwin-the-movie/utils/three-textures';
import { module, test } from 'qunit';

module('Unit | Utility | three-textures', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = threeTextures();
    assert.ok(result);
  });
});
