import cognusThreeComponent from 'bluedarwin-the-movie/utils/cognus-three-component';
import { module, test } from 'qunit';

module('Unit | Utility | cognus-three-component', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = cognusThreeComponent();
    assert.ok(result);
  });
});
